using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class scoreMap : MonoBehaviour
{
    // Start is called before the first frame update
    public static Dictionary<string, int> all_scores = new Dictionary<string, int>() { { "Ninja Frog", 500 }, {"Mask Dude", 500 } };
}
