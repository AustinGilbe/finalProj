using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scoreMap : MonoBehaviour
{
    // Start is called before the first frame update
    public static Dictionary<string, int> all_scores = new Dictionary<string, int>() { { "Ninja Frog", 0 }, {"Mask Dude", 0 } };
}
